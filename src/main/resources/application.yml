spring:
  application:
    name: code-mother

    # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/code-mother
    username: root
    password: dopa1997

server:
  port: 8123
  servlet:
    context-path: /api

springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.example.codemother.controller

knife4j:
  enable: true
  setting:
    language: zh_cn


